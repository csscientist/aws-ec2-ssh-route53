Provision EC2 instance on AWS by Terraform
Security gruop has been created and allow HTTP, HTTPS and SSH ports.
Key pair is created via (~/.ssh/...) file
AWS access key is connected by file.
httpd web server is installed and started on the instance
